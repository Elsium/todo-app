import {Todo} from '@/redux/Features/todosSlice'

interface PropsType {
    todo: Todo
}

const TaskForm = ({todo}: PropsType) => {

    return (
        <div>
            TaskForm
        </div>
    )
}

export default TaskForm