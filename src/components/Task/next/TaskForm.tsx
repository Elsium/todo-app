import {ITodo} from '@/redux/Features/todosSlice'

interface PropsType {
    todo: ITodo
}

const TaskForm = ({todo}: PropsType) => {

    return (
        <div>
            TaskForm
        </div>
    )
}

export default TaskForm